import React from 'react';
import HomeContainer from '../home_container'

const Home: React.FC = () => {
    return (
        <div>
            <HomeContainer/>
        </div>
    );
};

export default Home;